# MCTS Step 1 — Random Rollout Engine — 2026-04-13

## Task
LOG-1335: MCTS Implementation for Simulation AI — Step 1

## Commit
`8d3cabe` — `feat: random rollout engine for MCTS`

## File Created
`scripts/simulation/mctsAI.js`

## What Was Built
- `randomRollout(state, playerIdx, maxTurns)` — uniform random rollout, draws=loss
- `biasedRollout(state, playerIdx, maxTurns, policy)` + `DEFAULT_POLICY` — Step 3 pre-built
- `chooseActionMCTS(state, options)` — flat MCTS with UCB1 + immediate lethal detection
- `DEFAULT_POLICY` — exportable policy weights for evolutionary tuning (Step 4)

Built all 4 steps in one file (Steps 2-4 ready; Step 1 tested and committed).

## Test Results — 100 Rollouts per Matchup

| Matchup | P1 WR | Total Time | ms/rollout |
|---------|-------|------------|------------|
| elf vs beast | 0.0% | 1,239ms | 12.4ms |
| beast vs elf | 1.0% | 2,692ms | 26.9ms |
| beast vs human | 15.0% | 4,398ms | 44.0ms |
| human vs demon | 4.0% | 6,597ms | 66.0ms |

**Performance target (100 rollouts < 5s):** Met for light matchups. Heavy matchups
(Human/Demon) exceed at 66ms/rollout due to high action branching in uniform-random play.
Biased rollouts will reduce this significantly.

## Key Observations
- Random play P1 WR very low (0-15%): uniform random rarely attacks champions
- Win rates are sensible directionally (Beast beats Elf in most rollouts)
- No errors across all matchups
- 10s MCTS timeout safety valve ensures chooseActionMCTS never blocks forever

## Status
Step 1 complete. Awaiting board direction to proceed to Step 2.
